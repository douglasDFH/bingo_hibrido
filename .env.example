# ── API / Worker ──────────────────────────────────────────────
DATABASE_URL=postgresql://bingo:bingo@localhost:5433/bingo
REDIS_URL=redis://localhost:6379
JWT_SECRET=cambia-esto-en-produccion
# Carpeta raíz de archivos (PDFs subidos, imágenes generadas, banners)
DATA_DIR=./data
# Password del admin creado por el seed
ADMIN_PASSWORD=admin1234

# ── Solo docker-compose.prod ─────────────────────────────────
POSTGRES_USER=bingo
POSTGRES_PASSWORD=cambia-esto
POSTGRES_DB=bingo
